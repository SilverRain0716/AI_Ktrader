# CLAUDE.md

이 파일은 새 세션이 **맥락 없이 시작해서 같은 실수를 반복하지 않도록** 쓴 것이다.
현재 좌표는 [`docs/03-current-state.md`](docs/03-current-state.md) 에 있다. 그것부터 읽는다.

---

## 이 저장소의 실패 방식

지금까지 나온 결함은 성격이 거의 같았다. 이것만 기억하면 나머지는 따라온다.

> **동작하지 않는 것과 동작해서 통과하는 것은 겉으로 구분되지 않는다.**

실제로 있었던 일들이다.

- CI 가 `pytest -q || echo "..."` 로 테스트 실패를 삼켰다 — 임포트 에러로 **테스트가 한 건도 실행되지 않았는데 초록불이었다**
- 커버리지 가드가 **잘린 모수**를 세고 통과했다 (205 > 150, 그런데 모수가 314였다)
- 악재공시 필터가 **빈 테이블**을 조회했다 — 경고가 없었다
- 파서가 `—` 로 잘라 **근거가 들어 있는 쪽**을 버렸다
- 섹터 한도가 **'기타' 안에서** 무의미했다 (18%가 '기타'였다)
- 리스크 한도를 **아무도 설정하지 않은 채** 초안 기본값으로 돌았다
- `.env` 를 읽는 코드가 없었다 — `python-dotenv` 는 의존성에 선언만 돼 있었다
- 유량 한도 시험(B1)이 **10회 전부 200** 을 받았다. 8.9초 걸렸다 — 초당 한도에 **닿지도 못했다.** 연결을 미리 세우고 동시에 쏘니 경계가 나왔다(통과 10~11)

**그래서 작업 규칙이 하나 있다.**

고칠 때마다 **"그 상태로 되돌아가는 경로"를 실패하는 테스트로 막는다.**
현재 378건의 절반 이상이 그 목적으로 추가된 것이다.
값을 고쳤으면 그 값이 다시 틀렸을 때 빨간불이 켜지는지 **직접 확인**한다 — 통과하는 것을 보는 것으로는 부족하다.

---

## 하드 규칙 — 어기면 CI 가 막는다

이 저장소는 **public** 이다 ([ADR 0004](docs/adr/0004-repo-visibility.md)).

| # | 규칙 | 강제 |
|---|---|---|
| 1 | `execution/` 에 `.py` 가 들어오면 안 된다 (`__init__.py` 는 예외) | `repo-guard.yml` |
| 2 | `EXECUTION_MODE=live` · `KIWOOM_ENV=real` 을 커밋하지 않는다 | 같은 가드. `.py`·`.toml`·`.yml`·`.yaml` 만 훑고, `repo-guard.yml` 자신은 제외한다 — 다른 확장자는 사람이 본다 |
| 3 | **운용 자금 규모와 리스크 한도 값을 저장소에 남기지 않는다** | 사람이 지킨다. 코드·문서·주석·커밋 메시지 **전부** 해당된다 |
| 4 | `.env` 를 커밋하지 않는다 | pre-commit 훅 + `.gitignore` |
| 5 | 실계좌 주문 코드는 **킬 스위치 구현 전까지 머지 금지** | 사람이 지킨다 |

**`repo-guard.yml` 은 저장소가 private 이면 1·2번을 검사하지 않고 그대로 통과한다**(`if IS_PRIVATE ... exit 0`).
즉 private 으로 전환하는 순간 이 가드는 전부 꺼진다. 주문 코드가 들어오는 시점은 정확히 그 순간이다 —
**private 전환 이후에는 1·2번을 사람이 지킨다**고 생각해야 한다.

3번은 한 번 어겼다. `decision/config.py` 의 기본값과 `docs/01-context-pack-design.md` 의 예시에 실제 숫자가 있었다.
**코드에서 지우고 문서에 남겨두는 것은 절반만 지킨 것이다.**

---

## 작업 절차

```bash
pip install -e ".[dev,data]"
pre-commit install          # gitleaks · ruff · .env 차단
```

`main` 에 직접 커밋하지 않는다 (`no-commit-to-branch` 훅). 브랜치 → PR → 머지.

**커밋 전 검증 4종:**

```bash
pytest -q                          # 378 passed
ruff check . && ruff format --check .
python scripts/validate_schemas.py
mypy .                             # CI 에 없다. 대부분 스텁 누락 노이즈 — 건수가 늘지 않는지만 본다
```

---

## 데이터 계층 함정

실제로 당한 것들이다. 문서 조사로는 안 나온다.

| 함정 | 내용 |
|---|---|
| **수정주가 혼재** | 현재 일봉 소스는 네이버뿐이고 수정주가다(D3 로 실측). KRX·pykrx 계열은 `adjusted=False` 가 원본가라 **섞으면 액면분할 종목에서 수익률이 50배 튄다.** 소스를 추가할 때 축척부터 확인한다(E5 미결) |
| **거래정지일 0값 행** | 네이버 일봉은 정지일에 `시가=고가=저가=0` 행을 준다. 그대로 지표에 넣으면 ATR·볼린저가 오염된다. `open>0 & volume>0` 필터 필수 — 동시에 이게 정지일 판별 신호다 (`data/sources/naver.py` 의 `halted`, `data/store.py` 조회 조건) |
| **분봉은 소스마다 다르다** | 키움 `ka10080` 은 **13개월 소급 · OHLC 완비**(111p / 99,750행). 끝나는 이유는 날짜 컷오프가 아니라 **행 수 상한(약 10만)** 이다. 네이버는 대체재(D2n)일 뿐 — **롤링 창 4~6거래일 · 시·고·저가 전부 null**(종가는 온다), 그리고 거래량은 **당일 누적**이라 분봉 거래량은 연속 행의 차분이다. "분봉은 소급 불가"는 네이버를 보고 내린 판정이었다 |
| **FDR 소속부** | FDR `KRX-DESC` 의 컬럼 이름은 `Sector` 인데 **내용은 업종이 아니라 소속부**다(진짜 업종은 `Industry`). 저장소는 이걸 `dept` 로 바꿔 담는다(`data/sources/listing.py:210`). 코스닥 전용이라 **KOSPI 는 전부 결측**이고, 이 신호만으로 관리종목을 판정할 수 없다. 네이버 `isManagement` 와 **합집합**으로 쓴다 |
| **금액 단위** | 두 종류다. **지표·수급은 `_eok_krw`(억원)** — `adv20_eok_krw` · `market_cap_eok_krw` · `foreign_net_5d_eok_krw` · `inst_net_5d_eok_krw`. **계좌·손익·한도는 `_krw`(원)** — `cash_available_krw` · `total_equity_krw` · `unrealized_pnl_krw` · `realized_pnl_today_krw` · `daily_loss_limit_krw`. 브리핑 수급(`foreign_eok_krw` 등)도 억원이다. **접미사가 곧 단위다 — 필드를 추가할 때 이름을 맞춘다.** 억원 필드를 `_bil_krw`(십억) 로 잘못 읽으면 값이 **10배** 어긋난다 — 한 번 그랬다 |
| **거래일 vs 달력일** | 신선도는 **전부 거래일 기준**이다(`_sessions_missed`) — 달력일로 세면 금요일 배치 → 월요일 아침이 3일 낡음이 된다. 일봉은 2026-08-26 에, **수급은 2026-08-31 에** 고쳤다(그전까지 매주 월요일 수급 경고가 떴다). 신선도를 새로 추가할 때도 같은 함수를 쓴다 |
| **키움 가격의 부호** | `-257000` 은 **음수가 아니라 전일대비 하락 표시**다. 그대로 `int()` 하면 가격이 음수가 되고 수익률·지표가 조용히 뒤집힌다. 정본은 `data/sources/kiwoom.py` 의 `kiwoom_price()` 다(2026-08-30 프로브에서 옮김). 그리고 `SpotQuote.consistent` 가 **우리 계산 등락률을 거래소 `flu_rt` 와 대조**한다 — 부호를 안 떼면 여기 걸린다 |
| **분봉 백필은 동시성으로 푼다** | 순차로는 호출당 2.2초라 44시간이지만, **동시 8 이면 3.61콜/초(24/24 통과)로 약 5.7시간**이다(실측). 유량은 동시 10~11회가 경계이고 분·시간 누적 한도는 없다. 동시 12 는 4.9시간이나 24건 중 2건이 429 다 |
| **결정 스키마는 형식만 본다** | 전 필드가 `required` + nullable 이라 **`stop: null` 인 BUY 도 스키마는 통과한다.** action 별 필수 조건은 `decision/contract.py` 의 `action_requirements()` 가 막는다 — 예전 `allOf`/`if-then` 을 옮겨온 것이다. 스키마만 통과시키고 끝내면 손절 없는 진입이 나간다 |
| **장중 가격은 사이클마다 다르다** | premarket·postmarket 팩은 **전 거래일 종가가 맞는 값**이고, midday·preclose·event 는 키움 `ka10001` 현재가로 덮는다. 못 받으면 **덮은 척하지 않는다** — `data_quality.price_source` 가 `intraday`/`daily_close` 를 말하고 결손 사유가 경고에 남는다. 일부만 성공하면 **섞지 않고 통째로 전일 종가로 통일**한다 |
| **뉴스는 노이즈가 기본이다** | 네이버 종목뉴스는 **제목에 그 종목명이 없는 기사가 40~85%** 다(실측). 거르지 않고 `names_stock` 으로 표시만 한다 — 계열사 기사로 재료가 나오는 경우가 있고, 미리 버리면 무엇을 버렸는지 모른다. 대신 **종목명 달린 기사를 먼저** 정렬한다. 같은 사건은 같은 날 안에서 자카드 0.12 로 묶고 `cluster_size` 로 남긴다(14개 매체가 쓴 건도 있었다) |
| **컨텍스트는 채널이 아니다** | 뉴스·공시는 **후보를 만들지 않는다.** 이미 유니버스에 오른 종목에 붙고, **Arm 1·2 양쪽에 똑같이** 실린다 — 뉴스를 Arm 2 에서 빼면 `Arm1−Arm2` 가 '브리핑+뉴스' 증분이 되어 F3 가 바뀐다 (ADR 0010) |
| **`as_of` 상한** | 지표·거래정지·공시·브리핑 **전부** `as_of` 이하만 본다. 하나라도 빠지면 미래 정보가 유니버스에 샌다 |

---

## 시간축 — 이 시스템은 스윙이다

`max_hold_days` 1~30, 판단 4회/일, 추세추종. **단타가 아니다.**

이걸 놓치면 판단이 통째로 틀린다. 실측(2026-08-31 삼화콘덴서, ATR 9.9%):

| | ATR 대비 |
|---|---|
| 진입 시각 차이 (09:00 vs 09:10 vs 09:50) | **0.04~0.15배** |
| 하루 지수 급락으로 인한 갭 | 0.28배 |
| **손절까지 거리** | **2.0배** |

**하루치 움직임은 손절선까지 거리의 1/7 이하다.** 그래서:

- 진입 시각 최적화는 **2차적이다.** 갭 가드·손절 규칙이 먼저다 ([ADR 0009](docs/adr/0009-entry-timing.md) 실험 우선순위)
- 임계는 **고정 % 가 아니라 ATR 배수**로 잡는다 — ATR 4% 종목과 13% 종목에 같은 잣대를 대면 안 된다
- **임계는 종목마다 다른 잣대여야 한다.** 고정 % 를 두 번 틀렸다 — 갭 가드(ADR 0009)와 급변 스캔(ADR 0011) 둘 다.
  SK이노베이션 +4.38%(ATR 7.06% → 0.62배)는 고정 5% 에 안 걸리고, 원익홀딩스 −5.8%(ATR 11.1% → 0.52배)는 걸린다. **둘 다 틀린 판정이다**
- **"오늘 시장이 빠지니 관망"은 규율이 아니라 재량이다.** 하루 지수 하락은 stop 안에서 소화된다.
  [ADR 0006](docs/adr/0006-edge-hypothesis.md) 의 1차 엣지가 배제하려는 것이 바로 그 행동이고,
  F4(정의역 붕괴)는 사후 판정 기준이지 장중 재량의 근거가 아니다
- **그런데 시장 악재는 오히려 매수 근거가 될 수 있다** ([ADR 0013](docs/adr/0013-trading-doctrine.md) 원칙 4).
  위 문장이 막으려는 것은 *"무서우니까 안 산다"* 는 **근거 없는 회피**다.
  `sign=negative` AND `scope=market` AND **외국인·기관 순매수 > 0** 이면 저점 매수 후보다 —
  **세 값 모두 우리가 가진 것이고, 기계가 판정할 수 있으면 재량이 아니다**

---

## 판단 계층 규칙

- **리스크 한도 7개 + 페이퍼 시드 1개(`decision/config.py` 의 `_LIMIT_SPECS`)에는 기본값이 없다.** (`AIK_DATA_DIR` 같은 다른 `AIK_*` 는 기본값이 있다.) 하나라도 비거나, 숫자가 아니거나, 범위를 벗어나거나, 조합이 모순이면 `RiskLimitError` 로 팩 생성이 거부된다
- 종료 코드: **3** = 설정 문제(사람이 `.env` 를 고쳐야) · **1** = 팩을 만들 수 없음(대개 데이터 부족 — 배치를 돌리면 해결. `show` 에서는 그런 팩이 없다는 뜻) · **2** = 인자 문제
- **`constraints` 는 이제 강제된다.** `engine.validate()` 가 신규진입 수·종목비중·섹터합계(보유분 포함)·유동성·보유상한·손절종목·일일손실한도를 재검증하고 위반하면 **결정 전체를 거부하고 재요청**한다. `decision.schema.json` 도 소비자가 생겼다 — `output_config.format` 으로 API 에 넘겨 강제하고, 받은 뒤 다시 검증한다
- **회계 항등식**(`decision/positions.py` 의 `account_state`): `cash = 시드 − Σ취득원가 + Σ실현손익`. `total_equity − 현재평가금` 으로 현금을 구하면 하락장에서 매수 여력이 늘어난다
- **키움 유량은 문서와 다르고, 실전과 모의가 또 다르다.** 실전은 문서상 "초당 5회"인데 실측 **동시 10~11회**이고 5회/초 지속 300회가 전부 통과한다. **모의는 동시 3 고정 · 지속 약 2콜/초 상한**이다(문서의 "TR당 초당 1회"보다는 관대하다). 그래서 `KiwoomClient` 가 엔드포인트로 판정해 동시성과 **호출 간격**을 나눈다 — **동시성만 낮추면 부족하다**: 동시 2 라도 RTT 0.7초면 초당 2.8회가 나가 모의 상한을 넘는다(실제로 20종목 중 1종목이 429 였다). 그리고 **버킷이 TR 별로 분리**돼 있어 서로 다른 TR 을 병렬로 돌리면 처리량이 배가된다 — 다만 한 TR 만 쓰는 백필(분봉)에는 도움이 안 된다. **분·시간 누적 한도는 없다**(실전 1분 300회·5분 1,250회 전부 통과). 429 는 지속되지 않으므로 **레이트리미터는 하드 게이트가 아니라 재시도로 충분하다**
- **키움 토큰은 TTL 24시간이고, 재발급해도 같은 토큰이 돌아온다.** 프로세스 간 갱신 조율은 필요 없다. 대신 **강제 회전이 불가능**하다 — 키가 샜을 때 할 수 있는 일이 재발급이 아니라 폐기뿐이다
- **무엇을 근거로 사고 파는가의 정본은 [ADR 0013](docs/adr/0013-trading-doctrine.md) 이다.**
  채널 정의(momentum·flow·briefing)는 *구현된 것*이지 *의도된 것*이 아니었다 — 셋 다
  **이미 형성된 상태**만 보아서 SK이노베이션을 놓쳤다. 원칙과 채널이 어긋나면 원칙이 정본이다.
  **`stop` 은 없애지 않는다** — 포지션 크기가 손절폭에서 나온다. 주 청산은 `invalidation` 이고
  `stop` 은 재난 방지선이다. **`stop` 도달 빈도가 곧 `invalidation` 설계의 품질 지표다**
- `schemas/` 가 계층 간 계약이다. **지표를 추가할 때는 스키마를 먼저 고친다**
- **판단 엔진을 만들기 전에 [ADR 0007](docs/adr/0007-judgment-engine.md) 을 읽는다.** 호출은 저장소 안 **도구 없는** API 호출이고(외부 세션은 팩 밖을 봐서 F3 를 오염시킨다), 결정 저장소는 판단 엔진과 **같은 회차**에 만들며, `decision_id` 는 모델이 아니라 **러너가 생성**한다. 선행 작업 둘은 **끝났다**(2026-08-30) — 스키마는 구조화 출력에 그대로 넘길 수 있고(`output_config.format`), `pack.save()` 는 내용이 달라지는 덮어쓰기를 `PackImmutable` 로 거부한다(같은 내용 재빌드는 통과)

---

## 문서 지도

| 문서 | 언제 읽나 |
|---|---|
| [`docs/03-current-state.md`](docs/03-current-state.md) | **여기부터.** 지금까지 한 것과 남은 것 |
| [`docs/adr/`](docs/adr/) | 왜 그렇게 결정했는가. 0004(시크릿)·0005(백테스트 범위)·0006(엣지 가설)·0009(매매 시점)·0010(뉴스·컨텍스트)·0011(급변 스캔)·0012(뉴스 채널)·**0013(매매 원칙)**은 필독 |
| [`docs/01-context-pack-design.md`](docs/01-context-pack-design.md) | 팩 설계. **11장에 고친 결함이 누적된다** |
| [`docs/phase0-verification.md`](docs/phase0-verification.md) | 실측 체크리스트 35항목. 결과는 [`phase0-results.md`](docs/phase0-results.md) |

**결함 번호는 11장의 절 번호를 쓴다.** 예전 커밋의 "결함 N" 은 저장소 밖 점검 목록에서 온 것이라 추적되지 않는다.

---

## 지금 없는 것

`execution/` 과 `backtest/` 는 `.gitkeep` 뿐이다. **주문·스케줄러·알림·실시간 감시가 0줄이다.**

판단 엔진과 결정 저장소는 2026-08-30 에 생겼다(`decision/engine.py`, `decisions` 테이블) — 돌리려면 **쓰려는 제공자의 키**가 필요하다(`ANTHROPIC_API_KEY` 또는 `OPENAI_API_KEY`). **Arm 0(정량 랭킹)은 미구현이다** — ADR 0005 선결 과제 4가 풀리기 전까지 랭킹 규칙이 정의되지 않았고, 임의로 정하면 그것이 기준선이 된다.

**있어 보이지만 배선되지 않은 것들이 있다.** 손대기 전에 확인한다.

- ~~`KILL_SWITCH` · `EXECUTION_MODE`~~ **(2026-09-01 배선)** — `gate/` 패키지가 읽는다.
  **킬 스위치는 파일로도 켜진다**(`<데이터>/KILL`) — 환경변수는 프로세스 시작 시 고정돼
  돌고 있는 배치를 못 멈춘다. **해석할 수 없는 값은 '켜짐'으로 본다.**
  `python -m gate.pipeline status|check`. **게이트는 주문을 내지 않는다** — 판정만 한다
- `recent_decisions` 팩 블록 — `decisions` 테이블은 생겼지만 팩에 채우는 배선은 아직 없다
- ~~`invalidation_hit`~~ **(2026-08-31 배선)** — `decision/invalidation.py` + `python -m decision.pipeline watch [--apply]`. **감시기는 표시만 하고 청산하지 않는다** — 실행 계층이 0줄이라 킬 스위치 없는 자리에서 상태를 바꾸지 않는다. `stance_reversal` 과 뉴스 기반 재료 소멸은 **여전히 감시되지 않는다**
- `account.is_mock` — `decision/config.py` 에서 **상수 `True`**. `KIWOOM_ENV` 와 무관하며 **팩에 실려 AI 가 읽는다**
- 프로브의 `KIWOOM_REST_BASE` 기본값은 **모의 서버**인데, 기록된 A3·A4·B1·D2 측정치는 전부 **실전 서버**에서 나온 것이다. 환경변수 없이 다시 돌리면 그 숫자가 재현되지 않는다
- ~~키움 모의 앱키가 없다~~ **(2026-08-30 확보)** — 상시모의투자 국내주식 계좌 + 모의 전용 앱키. `KIWOOM_MOCK_APP_KEY`/`_SECRET` 로 `.env` 에 있다. **실전 키는 모의 서버에서 `8030` 으로 거부된다**

---

## 순서에 대한 경고

현 로드맵은 **Phase 6 에서 주문 코드와 그것을 막는 코드를 동시에 처음 쓴다.**
게이트는 그 순간까지 한 번도 검증된 적이 없다. Phase 6 은 모의투자지만, **게이트를 처음 시험하는 자리가 주문을 내보내는 자리와 같다**는 것이 문제다.

> 실행 계층에서 **주문만 제외한 것**을 먼저 만들고, 3개월 돌린 뒤, **주문 어댑터만 교체**한다.

자세한 재배치안은 [`docs/03-current-state.md`](docs/03-current-state.md) 4장에 있다.
**킬 스위치와 멱등성은 주문 코드보다 먼저 배선한다.** — 2026-09-01 에 `gate/` 로 그렇게 했다.
`execution/` 이 아니라 별도 패키지인 이유는 둘이다: public 인 동안 `repo-guard` 가 막고,
**게이트는 주문 어댑터와 독립이어야** 어댑터가 모의→실전으로 바뀌어도 그대로 쓸 수 있다.

**게이트가 잡은 실제 모순**: `.env` 가 `EXECUTION_MODE=paper` 인데 `KIWOOM_ENV=real` 이었다.
조회만 하던 동안은 무해했지만 모의투자를 켜는 순간 **실계좌로 주문이 나간다.**

**그런데 `KIWOOM_ENV` 는 읽는 코드가 없는 라벨이다.** 실제 서버는 `KIWOOM_REST_BASE` URL 이
정하고, `is_mock` 은 호스트에 `mockapi` 가 있는지로 판정한다(`data/sources/kiwoom.py`).
라벨을 보고 판정하면 게이트가 **거짓 안심**을 준다 — 그래서 게이트는 **URL 을 본다.**

**조회와 주문 엔드포인트를 나눴다.** 조회(`KIWOOM_REST_BASE`)는 실전이 낫다 — 데이터는 같은데
유량이 3.5배 관대하다. 주문(`KIWOOM_ORDER_BASE`)은 따로 둔다. 같은 값을 쓰면 **조회를
빠르게 하려다 주문이 실계좌로 간다.** 주문 URL 이 비어 있으면 게이트가 `mock`/`live` 를
막는다 — **조회용으로 조용히 대체하지 않는다.**
