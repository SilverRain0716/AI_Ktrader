# 지금까지 무엇을 했고, 무엇이 남았는가

작성: 2026-08-29 · 기준 `main` @ `98f5234` (08-27) · 커밋 74개 / PR 8건

이 문서는 **현재 좌표**를 적는다. 설계 의도가 아니라 코드를 읽어 확인한 사실만 쓴다.
다음에 이 저장소를 여는 사람(그게 나여도)이 "어디까지 왔지"를 다시 추적하지 않아도 되게 하는 것이 목적이다.

> **한 줄**
> 파이프라인은 컨텍스트 팩을 SQLite 에 저장하고 종료한다. **팩을 소비하는 코드가 아직 없다.**
> 지어진 세 계층은 견고하지만, 그 위에 얹힐 것이 한 줄도 없다.

---

## 1. 지금까지 한 것

### 1.1 병합된 작업

| PR | 날짜 | 내용 | 규모 |
|---|---|---|---|
| #1 | 08-22 | 유니버스 커버리지 — 모집단의 절반이 데이터 없이 빠져 있었다 | +330 |
| #2 | 08-22 | 악재공시 필터를 실제로 작동시킨다 — 그리고 방향을 본다 | +339 |
| #3 | 08-23 | 라벨이 없다고 근거가 없는 것은 아니다 | +473 |
| #4 | 08-23 | '기타'가 크면 섹터 한도는 아무것도 막지 못한다 | +278 |
| #5 | 08-26 | 1단계 — 팩 값을 믿을 수 있게 만든다 (치명 결함 7건) | +696 |
| #6 | 08-26 | 3·4단계 — 무엇을 백테스트할 수 있고, 왜 돈을 버는가 (ADR 0005·0006) | +290 |
| #7 | 08-27 | 리스크 한도를 필수로 만든다 — ADR 0004 위반 해소 | +569 |
| #8 | 08-27 | Phase 0 실측 도구 — 35칸 중 5칸을 앱키 없이 채운다 | +367 |

### 1.2 동작하는 계층

| 계층 | 규모 | 상태 |
|---|---|---|
| 데이터 적재 | 2,436줄 | 일봉 13개월 · 수급 1개월 · 공시 2개월. 모집단 632종목 커버리지 100% *(2026-08-23 적재 기준 — 이후 배치를 돌리지 않았다)* |
| 브리핑 구조화 | 1,002줄 | 관점 208건. 파서·재판정·경고 누적 |
| 컨텍스트 팩 | 1,383줄 | 스키마 검증 · 커버리지 가드 · 신선도 가드 · 회계 항등식 |
| 테스트 | 2,272줄 | 252건 통과 |

### 1.3 고친 결함

세 회차에 걸쳐 **결함 8건 + 치명 결함 7건(A~G) + 규정 위반 1건**을 닫았다.
저장소에 남은 기록은 [`01-context-pack-design.md`](01-context-pack-design.md) 11장(11.1~11.9)과 각 PR 본문이다.

> 커밋 메시지의 "결함 N" 번호는 **저장소 밖 점검 목록**에서 온 것이라 3번이 결번이다.
> 앞으로 결함 번호는 11장의 절 번호를 쓴다 — 저장소 안에서 추적 가능해야 한다.

특히 짚어둘 것:

- **현금이 평가금으로 오염**돼 하락장에서 매수 여력이 늘어나는 계산이었다. 물타기를 구조적으로 유도한다
- **브리핑 채널에 `as_of` 상한이 없어** 팩에 없는 미래 정보가 유니버스에 새어 들어왔다
- **신선도가 달력일 기준**이라 매주 월요일 세 사이클이 통째로 거부되고 있었다
- **금액 단위 필드명과 값이 달랐다**(`_bil_krw` 가 억원을 담음). 판단 주체가 AI 이므로 조용히 10배 틀릴 수 있었다
- **`.env` 를 읽는 코드가 저장소에 없었다.** `python-dotenv` 가 의존성에 선언만 돼 있었다

### 1.4 확정한 결정 (ADR)

| ADR | 결정 |
|---|---|
| 0005 | 백테스트는 **소급 가능한 원천만** 대상으로 한다. 브리핑·AI 증분은 페이퍼 3-arm 대응비교로 잰다 |
| 0006 | 엣지는 **1차로 규율, 2차로 선택**에서 나온다. 반증 조건 5개(F1~F5)에 관측 대상·판정 시점·행동을 붙였다. 다만 F1 의 임계(3%p)는 ADR 스스로 **임시값**이라 적었고, F2·F3 의 표본 계산은 불일치율 30% 가정 위에 있다 |
| 0002 후기 | 조건검색식을 배제한 근거 중 **둘이 브리핑에도 적용된다**는 것을 인정했다 |

### 1.5 Phase 0 실측 (08-27)

**35칸**(A1~F6) 중 **5칸**을 앱키 없이 채웠다. 전체는 [`phase0-results.md`](phase0-results.md).

> PR #8 과 초기 도구 주석에 "41칸"이라고 적었는데 오기였다. 실제 항목은 A 5 · B 5 · C 8 · D 6 · E 5 · F 6 = **35** 이다.

| # | 결과 | 의미 |
|---|---|---|
| **E1** ★ | 네이버 해외 IP **차단 없음** | 데이터 계층을 국내 서버로 옮길 이유가 없다 |
| **D2** ★ | 네이버 분봉 6거래일 · **OHLC 전부 null** | 종가·누적거래량만 쌓을 수 있다. 고저가는 키움 이후 |
| D3 | 네이버는 수정주가를 준다 | 원본가 소스와 섞으면 분할 종목에서 수익률이 튄다 |
| E3 | DART `status=000` | 한도 미도달 |
| E4 | FDR 상장폐지 최신 폐지일이 **전일** | 생존편향 보정에 쓸 만하다 |

---

## 2. 반복해서 드러난 한 가지

이 저장소에서 지금까지 나온 결함은 성격이 대부분 같았다.

> **동작하지 않는 것과 동작해서 통과하는 것은 겉으로 구분되지 않는다.**

- CI 가 테스트를 돌리지 않았다 — 초록불이었다
- 커버리지 가드가 **잘린 모수**를 세었다 — 통과했다
- 공시 필터가 **빈 테이블**을 조회했다 — 조용했다
- 파서가 **근거가 들어 있는 쪽**을 버렸다 — 경고가 없었다
- 섹터 한도가 **'기타' 안에서** 무의미했다 — 한도는 걸려 있었다
- 리스크 한도를 **아무도 설정하지 않은 채** 초안 값으로 돌았다 — 팩은 정상이었다

그래서 이 프로젝트의 작업 규칙이 하나 생겼다.
**고칠 때마다 "그 상태로 되돌아가는 경로"를 실패하는 테스트로 막는다.**
결함 수정 PR 들이 더한 것이 **134건 / 252건(53%)** 이다 (118 → 200 → 216 → 252).

---

## 3. 남은 것

### 3.1 아직 0줄인 것

`execution/` 과 `backtest/` 는 `.gitkeep` 뿐이다. git 히스토리 전체에서 파일이 추가된 적이 없다.

| 블록 | 무엇 | 없으면 |
|---|---|---|
| **A** 판단 엔진 | 팩 → LLM → 결정 JSON | 팩이 `context_packs` 테이블에 **판단에 쓰이지 않은 채** 쌓인다. `show`·`status` 로 사람이 들여다볼 뿐이다. **지금 상태다** |
| **B** 결정 저장소 | `decision_id` + 팩 스냅샷 + 결과를 append-only 로 | ADR 0003 이 **필터 방식**을 뒤집으며 든 문제 4번("AI가 자기 판단의 결과를 학습할 수 없다")을, 순서만 뒤집은 채 그대로 재현한다 |
| **C** 검증 게이트 | AI 출력을 제안으로 취급하고 재검사 | ADR 0006 의 1차 엣지(규율)가 정의상 미집행이다. **F5 는 지금 판정할 수 없다** — 주문도 포지션도 없어 위반 건수가 0 이다. 실패가 아니라 측정 불가다 |
| **D** 페이퍼 집행기 | 게이트 통과분을 `paper_positions` 에 자동 기록 | `open_position()` 은 있으나 **프로덕션 호출자가 없다**(테스트만 부른다). 3개월 페이퍼를 매일 네 번 손으로 돌려야 한다 |
| **E** 상주 실행·관측 | 스케줄러 · 로그 파일 · 실패 통보 | 자동매매가 아니라 수동 도구다. 배치가 죽어도 모른다 |
| **F** 키움 클라이언트 | 인증 · 잔고 · 주문 · 레이트리밋 큐 | 계좌 잔고가 `.env` 의 숫자 하나로 남는다 |
| **G** 실시간 감시·킬 스위치 | WebSocket · 무효화 조건 감시 | 진입은 자동인데 이탈은 수동인 상태 |

### 3.2 있어 보이지만 없는 것

설정·스키마·문서가 능력을 암시하는데 구현이 없는 것들. **지금은 무해하지만 실행 계층이 생기는 순간 그대로 실계좌에 실린다.**

| 겉보기 | 실제 |
|---|---|
| `KILL_SWITCH=false` | 읽는 코드가 없다. **true 로 바꿔도 아무 일도 일어나지 않는다** |
| `EXECUTION_MODE=paper` | 세 모드를 구분하는 코드가 없다 |
| `decision.schema.json` 142줄 | **생산자도 인스턴스 검증자도 없다.** CI 의 "JSON Schema 유효성 검사"는 스키마 **문법**만 본다 |
| `constraints` 블록 | "실행 계층이 재검증한다"고 적혀 있으나 재검증할 계층이 없다. **AI 에게 통보될 뿐 강제되지 않는다** |
| `invalidation` 컬럼 | `open_position()` 인자로 받긴 하지만 부르는 곳이 없고, **`invalidation_hit` 를 1로 바꾸는 UPDATE 가 저장소에 없다** |
| 계좌 잔고 | 증권사 조회가 없으므로 **사람이 `.env` 에 적은 숫자 하나** |

### 3.3 치명 위험

| | 위험 | 왜 |
|---|---|---|
| **R1** | 주문 코드와 그것을 막는 코드가 **동시에 태어난다** | 현 로드맵 Phase 6 이 그 배치다. 게이트는 그 순간까지 검증된 적이 없다. Phase 6 은 모의투자지만, 게이트를 처음 시험하는 자리가 주문을 내보내는 자리와 같다 |
| **R2** | 분봉 결손은 **복구 불가능**하다 | 소급 수집이 안 된다. 다만 네이버로 쌓을 수 있는 것은 종가·누적거래량뿐이다(D2 실측) |
| **R3** | 죽은 설정이 **안전 장치가 있다고 착각**하게 만든다 | "킬 스위치는 이미 있다"고 기억한 채 주문 코드를 붙이면 차단 수단 없이 가동된다 |
| **R4** | 규율이 강제되지 않는데 **규율을 엣지로 선언**했다 | 한도의 *형식·범위·조합*은 검사한다(PR #7). 다만 그것은 **값이 제정신인지**를 볼 뿐 **지켜지는지**를 보지 않는다 |
| **R5** | 실패가 사람에게 도달하지 않는다 | 로그가 콘솔로만 나간다 — 파일 핸들러도 알림도 없다. 지금은 사람이 직접 돌려서 보이지만 **스케줄러가 붙는 순간 그 성질이 사라진다** |
| **R6** | 브리핑 프롬프트가 저장소 밖에 있다 | 프롬프트가 바뀌면 과거와 현재 브리핑이 다른 함수의 출력이 되고, 검출할 방법이 없다 |

---

## 4. 순서를 바꿔야 한다

현 로드맵은 `3 백테스트 → 4 판단엔진 → 5 페이퍼 3개월 → 6 실행계층+게이트 → 7 실계좌` 다.
여기에 두 가지 문제가 있다.

- **5를 6보다 먼저 두면 페이퍼가 수동이 된다.** 포지션을 여는 함수는 있으나 부르는 코드가 없다
- **6에서 게이트와 주문이 동시에 태어난다** (R1)

두 문제의 해답이 같다.

> **실행 계층에서 주문만 뺀 것을 먼저 만들고, 3개월 돌린 뒤, 주문 어댑터만 갈아끼운다.**
> 그러면 게이트는 실주문을 만나기 전에 이미 3개월간 검증된 상태가 된다.

| 순서 | 내용 |
|---|---|
| **지금** | 앱키 발급 → Phase 0 나머지 실측 · 죽은 설정 정리(R3) · 브리핑 프롬프트 미러링(R6) |
| **1** | 판단 엔진 + 결정 저장소를 **같이**. 프롬프트는 저장소 안에 둔다 |
| **2** | 검증 게이트 + 페이퍼 집행기. **킬 스위치와 멱등성을 여기서 배선한다** — 주문 코드가 생기기 전에 |
| **3** | 스케줄러 + 알림. 자동화와 관측은 같이 온다 |
| — | *여기까지가 페이퍼 forward test 를 시작할 수 있는 최소 조건이다* |
| **4** | 페이퍼 3개월 · 백테스트 병행(다른 코드이므로 1~3과 병렬 가능) |
| **5** | 주문 어댑터 교체 → 모의투자. 새로 쓰는 것은 주문과 실시간 감시뿐이다 |
| **6** | 소액 실계좌. **F5(규율 미집행)가 통과된 뒤에만.** 한도 상향은 F2·F3 가 판정된 뒤다 — ADR 0006 은 그때까지 이 시스템을 검증되지 않은 상태로 본다 |

### 하지 말아야 할 것

- 백테스트를 건너뛰고 페이퍼 성과를 믿는 것 — 기준선이 없으면 AI 기여분을 분리할 수 없다
- 주문 코드와 게이트를 같은 회차에 쓰는 것 (R1)
- 판단 엔진만 만들고 결정 기록을 미루는 것 — 미룬 기간의 판단은 사후에 재현되지 않는다
- 알림 없이 스케줄러를 붙이는 것 (R5)
- 죽은 설정을 그대로 두는 것 — 지우거나 배선한다. "나중에 연결하겠다"가 가장 위험하다

---

## 5. 다음 한 걸음

**키움 앱키 발급.** 뒤의 모든 것이 여기에 막혀 있고, Phase 0 의 A·B·C·F 섹션이 전부 대기 중이다.

발급 후 `python scripts/phase0_probe.py` 로 A3(토큰 TTL)·A5(환경 동작)를,
`--probe-limits` 를 붙여 B1(유량 한도)을 채운다.

> 실전/모의 키는 서로 다르다. 그리고 **3개월간 실서버 미접속 시 자동 해지되는데 모의투자 접속은 카운트되지 않는다.**
> 모의투자로만 개발하면 조용히 해지된다.
