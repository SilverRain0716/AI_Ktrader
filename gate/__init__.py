"""집행 게이트 — 주문이 나가도 되는가를 판정한다. **주문 자체는 여기 없다.**

[CLAUDE.md](../CLAUDE.md) 의 순서 경고가 이 패키지의 존재 이유다.

> 현 로드맵은 **주문 코드와 그것을 막는 코드를 동시에 처음 쓴다.** 게이트는 그 순간까지
> 한 번도 검증된 적이 없다. … **킬 스위치와 멱등성은 주문 코드보다 먼저 배선한다.**

그래서 게이트를 먼저 만들고 **주문 없이** 검증한다. `execution/` 에 두지 않는 이유는 둘이다 —
저장소가 public 인 동안 `execution/` 에 `.py` 가 들어오면 `repo-guard` 가 막고(하드 규칙 1),
**게이트는 주문 어댑터와 독립이어야** 맞다. 어댑터가 모의→실전으로 바뀌어도 게이트는 그대로다.
"""
