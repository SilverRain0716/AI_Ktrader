name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # gitleaks 는 PR 커밋 범위를 git log 로 훑는다. 기본 depth 1 이면
          # 그 범위가 로컬에 없어 "unknown revision" 으로 죽고, 스캔이 0바이트로 끝난다.
          # 즉 depth 를 주지 않으면 시크릿 스캔은 PR에서 아예 동작하지 않는다.
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: 시크릿 스캔
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 의존성 설치
        # 테스트가 data.sources.listing(FinanceDataReader) 까지 import 하므로 data extra 도 필요하다.
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,data]"

      - name: Lint
        run: |
          ruff check .
          ruff format --check .

      - name: JSON Schema 유효성 검사
        run: python scripts/validate_schemas.py

      - name: 테스트
        # 실패를 삼키지 않는다. 지금까지의 버그를 고정해 둔 회귀 테스트가
        # 방어선 역할을 하려면 여기서 빌드가 깨져야 한다. (점검 2026-08-22 결함 1)
        run: pytest -q
